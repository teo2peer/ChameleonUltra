name: Python checks

on:
  pull_request:
    paths:
      - "software/**"
      - "firmware/application/src/app_cmd.c"
      - "firmware/application/src/app_status.h"
      - "firmware/application/src/data_cmd.h"
      - ".github/scripts/run_hardware_free_python_tests.py"
      - ".github/workflows/**"
  push:
    paths:
      - "software/**"
      - "firmware/application/src/app_cmd.c"
      - "firmware/application/src/app_status.h"
      - "firmware/application/src/data_cmd.h"
      - ".github/scripts/run_hardware_free_python_tests.py"
      - ".github/workflows/**"
  workflow_dispatch:

jobs:
  checks:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: software

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: |
            software/pyproject.toml
            software/uv.lock
            software/script/requirements.txt

      - name: Set up uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies with uv (if lockfile present)
        if: ${{ hashFiles('software/uv.lock') != '' }}
        run: uv sync --dev

      - name: Install tools with pip (fallback)
        if: ${{ hashFiles('software/uv.lock') == '' }}
        run: |
          python -m pip install --upgrade pip
          # Try project requirements if present
          if [ -f script/requirements.txt ]; then pip install -r script/requirements.txt || true; fi
          # Ensure ruff and pyrefly are available
          pip install ruff pyrefly

      - name: Hardware-free unit tests
        run: |
          if [ -f uv.lock ]; then
            uv run python ../.github/scripts/run_hardware_free_python_tests.py
          else
            python ../.github/scripts/run_hardware_free_python_tests.py
          fi

      - name: Ruff check
        run: |
          set -e
          (uv run ruff --version && uv run ruff check .) || ruff check .

      - name: Pyrefly check
        run: |
          set -e
          (uv run pyrefly --help >/dev/null 2>&1 && uv run pyrefly check) || pyrefly check
